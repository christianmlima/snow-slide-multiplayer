{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd shared && npm install && cd ../client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite"
}
