{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd shared && npm install && cd ../server && npm install"
  },
  "deploy": {
    "startCommand": "cd server && npx tsx src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
